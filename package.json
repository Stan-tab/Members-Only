{
	"main": "app.js",
	"dependencies": {
		"bcryptjs": "^3.0.2",
		"connect-pg-simple": "^10.0.0",
		"dotenv": "^17.2.2",
		"ejs": "^3.1.10",
		"express": "^5.1.0",
		"express-session": "^1.18.2",
		"passport": "^0.7.0",
		"passport-local": "^1.0.0",
		"pg": "^8.16.3"
	},
	"scripts": {
		"initdb": "node db/populated.js && npm run dev",
		"dev": "node --watch $( node -p \"try { require('./package.json').main } catch(e) {console.error(e)}\")"
	}
}
